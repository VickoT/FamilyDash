services:
  dash:
    build: .
    ports:
      - "8050:8050"
    env_file:
      - .env
    restart: unless-stopped

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile.scheduler
    volumes:
      - ./data:/app/data
    restart: unless-stopped
